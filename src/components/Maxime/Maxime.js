
import React from 'react'
import PropTypes from 'prop-types'

import * as Styled from './__styles__/Maxime.styles.js'

const Maxime = ({  ,  }) => <div>Maxime</div>

Maxime.propTypes = {
  : PropTypes.Object.isRequired,
  : PropTypes.Object.isRequired
}

Maxime.defaultProps = {
  : {},
  : {}
}

export default Maxime
